# Se connecter à Azure Cloud Shell, copier les fichiers de ce réprertoire dans un répertoire ('azure-demo' par exemple, et exécuter le code ci-dessous:

$ terraform init

$ terraform plan

$ terraform apply

# Nous pouvons ensuite aller dans les ressources de l'abonnement utilisé pour voir les ressources créées.

# Pour détruire le déploiement:
$ terraform destroy
